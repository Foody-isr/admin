// The four top-level sections on the menu-item edit/create pages. Kept as a
// standalone type so `MenuItemTabBar` and the pages share a single source.
export type MenuItemSection = 'details' | 'modifiers' | 'recipe' | 'cost';
